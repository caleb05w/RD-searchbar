import SearchableTopicsList from "./components/SearchableTopicsList";

export default function App() {
  return (
    <div className="size-full flex items-center justify-center" style={{ backgroundColor: '#F8F8F8' }}>
      <SearchableTopicsList />
    </div>
  );
}